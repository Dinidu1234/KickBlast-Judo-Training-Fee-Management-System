<UserControl x:Class="KickBlastEliteUI.Views.CalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Fee Input" FontWeight="SemiBold" FontSize="16"/>
                <ComboBox Margin="0,8" ItemsSource="{Binding Athletes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedAthlete}"/>
                <TextBlock Text="Coaching hours per week (0-5)"/>
                <TextBox Margin="0,4,0,8" Text="{Binding CoachingHours, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Competitions this month"/>
                <TextBox Margin="0,4,0,8" Text="{Binding CompetitionsCount, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Calculate &amp; Save" Command="{Binding CalculateAndSaveCommand}"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Invoice" FontWeight="SemiBold" FontSize="20"/>
                <TextBlock Text="{Binding SelectedAthlete.Name, StringFormat='Athlete: {0}'}" Margin="0,6"/>
                <TextBlock Text="{Binding SelectedAthlete.TrainingPlan.Name, StringFormat='Plan: {0}'}"/>
                <Separator Margin="0,8"/>
                <TextBlock Text="{Binding TrainingCost, StringFormat='Training Cost: {0}'}"/>
                <TextBlock Text="{Binding CoachingCost, StringFormat='Coaching Cost: {0}'}"/>
                <TextBlock Text="{Binding CompetitionCost, StringFormat='Competition Cost: {0}'}"/>
                <TextBlock Text="{Binding TotalCost, StringFormat='TOTAL: {0}'}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,10,0,0"/>
                <TextBlock Text="{Binding WeightStatus, StringFormat='Weight Status: {0}'}" Margin="0,10,0,0"/>
                <TextBlock Text="{Binding SecondSaturday, StringFormat='2nd Saturday: {0}'}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
