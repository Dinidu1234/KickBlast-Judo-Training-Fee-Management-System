<UserControl x:Class="KickBlastEliteUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel Width="420">
            <TextBlock Text="Pricing Settings" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="Beginner Weekly Fee" Margin="0,8,0,0"/>
            <TextBox Text="{Binding BeginnerWeeklyFee, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Intermediate Weekly Fee" Margin="0,8,0,0"/>
            <TextBox Text="{Binding IntermediateWeeklyFee, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Elite Weekly Fee" Margin="0,8,0,0"/>
            <TextBox Text="{Binding EliteWeeklyFee, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Competition Fee" Margin="0,8,0,0"/>
            <TextBox Text="{Binding CompetitionFee, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Coaching Hourly Rate" Margin="0,8,0,0"/>
            <TextBox Text="{Binding CoachingHourlyRate, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Reset Database" Command="{Binding ResetDatabaseCommand}" Background="#B91C1C"/>
                <Button Content="Save Pricing" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
