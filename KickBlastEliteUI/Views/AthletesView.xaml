<UserControl x:Class="KickBlastEliteUI.Views.AthletesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <ComboBox Margin="0,0,0,8" SelectedValue="{Binding FilterPlan}">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Beginner"/>
                <ComboBoxItem Content="Intermediate"/>
                <ComboBoxItem Content="Elite"/>
            </ComboBox>
            <DataGrid ItemsSource="{Binding Athletes}" SelectedItem="{Binding SelectedAthlete}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Plan" Binding="{Binding TrainingPlan.Name}"/>
                    <DataGridTextColumn Header="Current Weight" Binding="{Binding CurrentWeight}"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding CompetitionCategoryWeight}"/>
                    <DataGridTemplateColumn Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#DC2626"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Add / Edit Athlete" FontSize="16" FontWeight="SemiBold"/>
                <TextBox Margin="0,6" Text="{Binding Editor.Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                <ComboBox Margin="0,6" ItemsSource="{Binding Plans}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding Editor.TrainingPlanId, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                <TextBox Margin="0,6" Text="{Binding Editor.CurrentWeight, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                <TextBox Margin="0,6" Text="{Binding Editor.CompetitionCategoryWeight, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="New" Command="{Binding NewCommand}" Background="#475569"/>
                    <Button Content="Save" Command="{Binding SaveCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
