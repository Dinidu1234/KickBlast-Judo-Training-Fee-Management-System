<UserControl x:Class="KickBlastStudentUI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="200" ItemsSource="{Binding Athletes}" SelectedItem="{Binding SelectedAthlete}" DisplayMemberPath="Name" Margin="0,0,8,0"/>
                <ComboBox Width="90" ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Margin="0,0,8,0"/>
                <ComboBox Width="100" ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Margin="0,0,8,0"/>
                <Button Content="Apply" Command="{Binding ApplyFilterCommand}"/>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1" Margin="0,0,0,8">
            <DataGrid ItemsSource="{Binding Calculations}" SelectedItem="{Binding SelectedCalculation}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding CalculationDate, StringFormat=yyyy-MM-dd}" Width="120"/>
                    <DataGridTextColumn Header="Athlete" Binding="{Binding Athlete.Name}" Width="*"/>
                    <DataGridTextColumn Header="Competitions" Binding="{Binding CompetitionsThisMonth}" Width="110"/>
                    <DataGridTextColumn Header="Total (LKR)" Binding="{Binding TotalCost, StringFormat=N2}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="2">
            <TextBlock Text="{Binding Details}" TextWrapping="Wrap" FontFamily="Consolas"/>
        </Border>
    </Grid>
</UserControl>
