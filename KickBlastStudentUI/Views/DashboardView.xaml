<UserControl x:Class="KickBlastStudentUI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource CardStyle}" Grid.Column="0" Margin="0,0,12,0">
            <StackPanel>
                <TextBlock Text="Athletes" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="{Binding TotalAthletes}" FontSize="40" FontWeight="Bold" />
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="6,0">
            <StackPanel>
                <TextBlock Text="Calculations" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="{Binding TotalCalculations}" FontSize="40" FontWeight="Bold" />
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Column="2" Margin="12,0,0,0">
            <StackPanel>
                <TextBlock Text="Plans" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="{Binding TotalPlans}" FontSize="40" FontWeight="Bold" />
                <TextBlock Text="{Binding LastUpdated, StringFormat='Updated: {0:yyyy-MM-dd HH:mm}'}" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
