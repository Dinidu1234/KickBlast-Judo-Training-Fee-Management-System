<UserControl x:Class="KickBlastStudentUI.Views.CalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource CardStyle}" Grid.Column="0">
            <StackPanel>
                <TextBlock Text="Monthly Fee Calculator" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="Athlete"/><ComboBox ItemsSource="{Binding Athletes}" SelectedItem="{Binding SelectedAthlete}" DisplayMemberPath="Name"/>
                <TextBlock Text="Competitions This Month" Margin="0,8,0,0"/><TextBox Text="{Binding CompetitionsThisMonth}"/>
                <TextBlock Text="Coaching Hours / Week (0-5)" Margin="0,8,0,0"/><TextBox Text="{Binding CoachingHoursPerWeek}"/>
                <TextBlock Text="{Binding BeginnerNote}" Foreground="#B7791F" Margin="0,8,0,0" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Calculate" Command="{Binding CalculateCommand}" Margin="0,0,8,0"/>
                    <Button Content="Save" Command="{Binding SaveCalculationCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Column="2">
            <TextBlock Text="{Binding ResultText}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="14"/>
        </Border>
    </Grid>
</UserControl>
